import { TournamentEnrollment } from './tournament-enrollment';

describe('TournamentEnrollment', () => {
  it('should create an instance', () => {
    expect(new TournamentEnrollment()).toBeTruthy();
  });
});
