import { TournamentMoreEnrollments } from './tournament-more-enrollments';

describe('TournamentMoreEnrollments', () => {
  it('should create an instance', () => {
    expect(new TournamentMoreEnrollments()).toBeTruthy();
  });
});
